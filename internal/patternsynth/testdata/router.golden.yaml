# Generated by `polyflow pattern synth` — do not edit by hand.
# Re-synthesize with:
#   polyflow pattern synth --package github.com/example/orion/router --corpus testdata/corpus
# package: github.com/example/orion/router
# corpus:  testdata/corpus
# corpus-sha: sha256:c8f8b62e5bb579eb
# proposer: template
#
# Every capture role below is inferred, not reviewed. Check the roles
# before this file is trusted, and add the fixture directory
# patterns/go/router_test/ that TestPatternFixtures requires.
language: go
version: "1"
package: github.com/example/orion/router
patterns:
  - name: router_get_route
    query: |
      (call_expression
        function: (selector_expression
          operand: (identifier) @receiver
          field: (field_identifier) @method
          (#match? @method "^(Delete|Get|Post|Put)$"))
        arguments: (argument_list
          (interpreted_string_literal) @path
          (identifier) @handler))
    captures:
      - name: receiver
        role: receiver
      - name: method
        role: http_method
      - name: path
        role: http_path
      - name: handler
        role: handler_ref
  - name: router_route_group
    query: |
      (call_expression
        function: (selector_expression
          operand: (identifier) @receiver
          field: (field_identifier) @method
          (#eq? @method "Group"))
        arguments: (argument_list
          (interpreted_string_literal) @prefix
          (func_literal) @_body))
    captures:
      - name: receiver
        role: receiver
      - name: method
        role: method
      - name: prefix
        role: route_prefix
