function noise(configId, uri) {
  doSomething(uri).then(function () {});
  axios.notAVerb(uri);
  axios({ method: "GET", path: uri });
  axios({ headers: uri });
}

// An object literal whose methods are not transports: no wrapper fact.
const Format = {
  trimIt: (s) => trim(s),
  render(s) {
    return draw(s);
  },
};
