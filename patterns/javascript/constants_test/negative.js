const count = 42;
let items = [1, 2, 3];
var legacy = "var declarations are not lexical"; // variable_declaration, not lexical
const numericRoutes = { home: 1, settings: 2 }; // pair values are not strings
