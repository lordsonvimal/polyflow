// create with no baseURL — does not match axios_create_with_baseurl
const noBase = axios.create({ timeout: 5000 });

// non-HTTP method names — does not match axios_instance_call
obj.use(middleware);
obj.connect(host, port);
arr.map(fn);

// calls with no URL argument — does not match axios_instance_call
obj.get();
