# Module with only literal assignments — no function definitions, no class
# definitions, no function calls, and no import statements.
# Zero pattern matches expected.
MAX_RETRIES = 3
DEFAULT_TIMEOUT = 30.0
EMPTY_DICT = {}
ITEMS = [1, 2, 3]
MESSAGE = "hello"
CONFIG = {"debug": False, "port": 8080}
