-- CREATE TABLE fake_table (id INT); this is just a comment, not real SQL
/* another comment mentioning CREATE VIEW fake_view AS SELECT 1
   and ALTER TABLE fake ADD COLUMN x INT; and DROP TABLE fake; */
